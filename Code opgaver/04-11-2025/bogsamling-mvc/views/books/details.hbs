<h1>{{book.title}}</h1> 
<p>Forfatter: {{book.author}}</p>
<p>Udgivelsesår: {{book.year}}</p>
    {{#if book.image}}
        <p>ImageName: {{book.image.filename}}</p>
    {{/if}}
<a href="/books/{{book.id}}/edit"><button>Rediger</button></a>
<form action="/books/{{book.id}}/delete" method="POST" style="display:inline;">
    <button type="submit" onclick="return confirm('Er du sikker?')">Slet</button>
</form>
<br>
<a href="/books"><button>Tilbage til alle bøger</button></a>